
  # Low-Fidelity Food Sharing App

  This is a code bundle for Low-Fidelity Food Sharing App. The original project is available at https://www.figma.com/design/oPIMJr95cau0QPigJFj0Lv/Low-Fidelity-Food-Sharing-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # FoodWaste
